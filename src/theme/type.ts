export type Theme = {
  color: string;
  background: string;
  chess: {
    normal: string;
    hover: string;
    active: string;
  };
  button: {
    normal: string;
    hover: string;
    active: string;
  };
  loader: string;
};
